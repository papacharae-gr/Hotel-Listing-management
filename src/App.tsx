import { ListingUpdatePage } from './features/listings';


export default function App() {
// For now we hard-code the id; later we can plug this into a router
return <ListingUpdatePage id="listing-123" />;
}